#pragma once

template <typename TKey, typename TValue>
class MyDicitionary
{
public:

	MyDictionary();


	bool insert(std::pair<TKey, TValue>)
	{

	};

	bool deleteValue(TKey kyValue)
	{

	};

	TValue search(TKey keyValue)
	{

	};

private:

};